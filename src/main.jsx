import React from "react";
import ReactDOM from "react-dom/client";
import "./styles/auth.css";
import { SituApp } from "./SituApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SituApp />
  </React.StrictMode>
);
