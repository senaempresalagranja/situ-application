import React from "react";
import { Router, Routes } from "react-router-dom";

export const AppRouter = () => {
  return (
    <Routes>
      <Router></Router>
      <Router></Router>
    </Routes>
  );
};
